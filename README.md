# K6-API-Load-Testing-Framework
Config-driven k6 load testing framework that benchmarks two implementations of the same .NET API side by side — profiles, custom metrics, thresholds, HTML reports, and CI.
